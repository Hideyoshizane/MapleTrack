<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="css/login.css">
    <title>Home</title>
    <script type="text/javascript" src="../public/javascript/login.js"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@100;200;300;400;500;600;700;800&display=swap" rel="stylesheet">
</head>
<body>
   
    <div class = "logo">
        <img src="assets/logo/logo.webp"id ="logo">
        <form action="/login" method="post">
            <% if (flash && flash.type == 'success') { %>
                <div class="message <%= flash.type %>">
                  <%= flash.message %>
                </div>
              <% } %>
            <input id="username" class="username" type="text" placeholder="Username" name="username" required>
            <input id="password" class="password" type="password" placeholder="Password" name ="password" required>
            <a href = "home.ejs">
                <p class= "forgotPassword">Forgot password?</p>
            </a>
            <% if (flash && flash.type == 'failed') { %>
                <div class="message <%= flash.type %>">
                  <%= flash.message %>
                </div>
              <% } %>
            <input class ="login" type="submit" value="Login">
        </form>
        
        <button onclick="location.href='/signup'" class="noAccount">Don't have an account? Register!</button>
    </div>
   
      
</body>
</html>